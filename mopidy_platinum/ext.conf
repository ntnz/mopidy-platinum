[platinum]
enabled = true
# Only responsible for catching track/queue/art changes, not the ticking
# time display or queue-fill/AirPlay progress (those live in their own
# fast-refreshing frame) -- kept high so the whole page, album art included,
# doesn't reload/flash more often than it has to on period-appropriate
# browsers.
refresh_interval = 60
status_refresh_interval = 2
max_list_items = 200
airplay_device_name =
