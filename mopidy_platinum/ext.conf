[platinum]
enabled = true
# Now only responsible for catching track/queue/art changes, not the ticking
# time display (that lives in its own fast-refreshing frame) -- bumped up
# from 15 so album art doesn't reload/flash as often.
refresh_interval = 30
status_refresh_interval = 2
max_list_items = 200
airplay_device_name =
